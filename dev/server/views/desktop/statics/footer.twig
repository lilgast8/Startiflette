	
	</div>  <!-- End #page-container -->
	
	<!-- Footer -->
	<footer id="footer">
		{% if Lang.MULTI_LANG %}
		<nav class="footer-lg">
			{% for lang, altLangUrl in Router.ALT_LANG_URL %}
			<a href="{{ altLangUrl }}" class="footer-lg-link" data-lg="{{ lang }}">{{ lang }}</a>
			{% endfor %}
		</nav>
		{% endif %}
		<nav>
			<ul>
				<li>
					<a href="{{ Router.LINK.statics.legal_notices }}" class="footer-link">
						{{ menu.legals }}
					</a>
				</li>
			</ul>
		</nav>
	</footer>
	
	{{ include( 'no-js.twig' ) }} {# no JS #}
	
	{{ include( 'old-browser.twig' ) }} {# old browser #}
	
</div> <!-- End #main-container -->



{{ include( 'js-var.twig' ) }} {# JS var #}

{#<?php

/* JS var */
include_once Path::$FILE->shared . 'js-var.php';

/* JS scripts */
echo $this->path->getJsFilesUrl( 'scripts-desktop' );

?>
#}


</body>
</html>